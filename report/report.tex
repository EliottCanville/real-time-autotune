\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{float}

\geometry{margin=2.5cm}

\title{Real-Time Signal Processing \\
\large Autotune Project Report}
\author{Your Names Here}
\date{January 2025}

\begin{document}

\maketitle

\section{Introduction}

This report presents the implementation and analysis of a real-time autotune audio effect using additive synthesis. The project was developed in C/C++ using the RtAudio API for real-time audio processing.

\section{Getting Started with RtAudio API}

\subsection{Question 2: Functionality of audioprobe and duplex}

\textbf{What do these two programs do?}

\texttt{audioprobe} scans and displays information about available audio devices (channels, sampling rates, formats, device IDs). It allows identification of system audio capabilities before configuring a stream.

\texttt{duplex} implements a real-time pass-through system that captures audio from an input device and plays it back simultaneously through an output device. It demonstrates the basic RtAudio callback structure used for real-time processing.

\subsection{Question 3a: Roles of openStream() and inout()}

\textbf{What are the roles of these two functions? Identify the arguments of openStream() and inout() for exchanging data between main and audio callback.}

\texttt{openStream()} configures and initializes the audio stream by setting device parameters, audio format (RTAUDIO\_FLOAT64), sampling rate, buffer size, callback function pointer, and user data. It prepares the audio processing pipeline before real-time execution.

\texttt{inout()} is the callback function called automatically at regular intervals to process audio in real-time. It reads samples from the input buffer, performs signal processing, and writes to the output buffer within strict timing constraints to avoid dropouts.

The key arguments for data exchange are \texttt{void *outputBuffer} (data to speakers), \texttt{void *inputBuffer} (data from microphone), and \texttt{void *userData} (user-defined structure passed from main containing buffer sizes, audio files, or dump buffers). These void pointers must be cast to their actual types within the callback.

\section{Debugging Toolbox}

\subsection{Question 4: Loading Audio Files}

\textbf{Where do you place the loading of the audio file in the program, and why?}

Audio file loading is placed in \texttt{main()} before starting the stream. File I/O operations (fopen, fread) are blocking and non-deterministic, taking unpredictable time based on disk performance. Performing them in \texttt{inout()} would cause buffer underruns and dropouts. The file only needs loading once at initialization, and memory allocation must occur before real-time processing to avoid non-deterministic behavior. By loading in main() and passing a pointer via userData, the entire file becomes available to all callbacks for sequential access.

\subsection{Question 7: Buffer Initialization}

\textbf{Do you initialize it in main() or inout() and why?}

\texttt{buffer\_dump} is initialized in \texttt{main()} before starting the stream. Dynamic memory allocation (malloc, calloc) is non-deterministic and causes delays leading to buffer underruns if performed in the callback. The buffer must persist across all callbacks to accumulate data; initializing in \texttt{inout()} would recreate it each call, losing previous data. Initialization in main() allows proper cleanup with free() at termination. A pointer is passed via userData for incremental writing during callbacks.

\subsection{Question 9: Writing to Disk}

\textbf{Do you write it in main() or inout() and why?}

\texttt{buffer\_dump} is written to disk in \texttt{main()} after stopping the stream. File operations (fopen, fwrite, fclose) are blocking and non-deterministic, taking significant time based on file size and disk performance. Performing them in \texttt{inout()} would cause severe underruns compromising real-time operation. Writing in main() ensures complete data capture in one operation rather than multiple partial writes. During callbacks, data is only copied to the buffer using \texttt{write\_buff\_dump()} (fast, deterministic), while the actual disk write occurs once at program end, separating real-time and non-real-time operations.

\section{Signal Analysis}

\subsection{Question 12: Fundamental Frequency Estimation}

\textbf{Include in your report your plots of $f_0$ as well as the files containing the corresponding input signals. Comment on your results.}

The fundamental frequency $f_0$ was estimated using the autocorrelation method. For each input buffer, the autocorrelation function was computed, and the second maximum was identified to determine the period of the signal, from which $f_0$ was calculated as:

\begin{equation}
f_0 = \frac{f_s}{\tau}
\end{equation}

where $f_s$ is the sampling rate (44100 Hz) and $\tau$ is the lag corresponding to the second maximum of the autocorrelation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{F01_a3_s100_v04.png}
    \caption{Input signal: female sustained vowel F01\_a3\_s100\_v04}
    \label{fig:input_signal}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{f0-without_frequency_range-512.png}
    \caption{$f_0$ without frequency range limitation (N=512)}
    \label{fig:f0_no_limit}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{f0-512.png}
    \caption{$f_0$ with frequency range 80-450 Hz (N=512)}
    \label{fig:f0_limited}
\end{figure}

\textbf{Analysis and comments:}

Figure \ref{fig:input_signal} shows a female sustained vowel with clear periodic structure. Without frequency range limitation (Figure \ref{fig:f0_no_limit}), the autocorrelation produces erratic estimates exceeding 1000 Hz, demonstrating the critical need for constraints. Low-amplitude regions trigger arbitrary detections.

With 80-450 Hz limitation (Figure \ref{fig:f0_limited}), estimation improves dramatically. The trajectory stabilizes around 200-250 Hz, consistent with female vocal range. Octave errors ($f_0/2$, drops to ~100-125 Hz) persist during transitions when autocorrelation peaks correspond to twice the fundamental period.

\subsection{Question 14: Buffer Size Analysis and Circular Buffer}

\textbf{Provide the corresponding plots and comment. Why would a circular buffer be useful here?}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{f0-128.png}
    \caption{$f_0$ estimation with buffer size N=128}
    \label{fig:f0_128}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{f0-256.png}
    \caption{$f_0$ estimation with buffer size N=256}
    \label{fig:f0_256}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{f0-512.png}
    \caption{$f_0$ estimation with buffer size N=512}
    \label{fig:f0_512}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{f0-1024.png}
    \caption{$f_0$ estimation with buffer size N=1024}
    \label{fig:f0_1024}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{f0-2048.png}
    \caption{$f_0$ estimation with buffer size N=2048}
    \label{fig:f0_2048}
\end{figure}

\textbf{Analysis of buffer size effects:}

For the female vocal at ~200 Hz, at least ~220 samples are required for one complete period at 44100 Hz. With N=128 (Figure \ref{fig:f0_128}), the window is too short, producing extreme noise and persistent octave errors. N=256 (Figure \ref{fig:f0_256}) remains highly erratic with frequent detection failures.

At N=512 (Figure \ref{fig:f0_512}), the trajectory stabilizes with reduced jitter but occasional octave jumps during transitions. N=1024 (Figure \ref{fig:f0_1024}) provides significantly smoother tracking with fewer errors. N=2048 (Figure \ref{fig:f0_2048}) yields the most stable estimation but reduces temporal resolution (~21 Hz update rate) and increases latency (~46 ms), problematic for real-time interaction.

\textbf{Why a circular buffer is useful:}

A circular buffer decouples analysis from I/O buffer sizes, enabling large analysis windows (e.g., 1024 samples) with small I/O buffers (e.g., 256 samples) for both low latency and accurate estimation. It creates a sliding window by automatically overwriting old samples, avoiding explicit data shifting. Overlapping window analysis produces smoother $f_0$ trajectories with better temporal tracking. Fixed memory allocation and pointer arithmetic ensure efficient real-time processing.

\section{Harmonic Analysis and Additive Synthesis}

\subsection{Question 15: DFT Bin Calculation}

\textbf{What is the value of the DFT bin closest to the measured $f_0$?}

For a DFT of size $n_{fft}$, the frequency resolution is $\Delta f = f_s / n_{fft}$. The bin index $k$ corresponding to a frequency $f$ is:

\begin{equation}
k = \text{round}\left(\frac{f}{\Delta f}\right) = \text{round}\left(\frac{f \cdot n_{fft}}{f_s}\right)
\end{equation}

For $f_0 \approx 200$ Hz with $f_s = 44100$ Hz and $n_{fft} = 2048$ (next power of 2 from buffer size), the bin is $k_0 = \text{round}(200 \times 2048 / 44100) \approx 9$.

\subsection{Question 16: Harmonic Extraction}

\textbf{Calculate the DFT and extract frequency and amplitude values for each harmonic.}

The FFT is calculated on the input buffer (zero-padded to $n_{fft}$). For each harmonic $h = 1, 2, \ldots$, the bin index is $k_h = h \cdot k_0$. The complex DFT value at bin $k_h$ is $X[k_h] = R[k_h] + jI[k_h]$.

The amplitude is obtained from the magnitude, normalized by $n_{fft}$:
\begin{equation}
A_h = \frac{2}{n_{fft}} \sqrt{R[k_h]^2 + I[k_h]^2}
\end{equation}

The factor 2 accounts for the fact that a real signal's DFT has symmetric positive and negative frequencies. The phase is $\phi_h = \text{atan2}(I[k_h], R[k_h])$.

\subsection{Question 17: Additive Synthesis Without Phase Continuity}

\textbf{Implement synthesis and explain the clicks heard.}

Additive synthesis reconstructs the signal by summing cosines at harmonic frequencies with extracted amplitudes:
\begin{equation}
y[n] = \sum_{h} A_h \cos\left(2\pi f_h \frac{n}{f_s}\right)
\end{equation}

When each frame is synthesized independently (resetting $n=0$ at each callback), the phase is discontinuous at frame boundaries. If the signal value at the end of frame $k$ differs from the value at the start of frame $k+1$, an abrupt jump occurs, producing audible clicks. These discontinuities introduce high-frequency artifacts.

\subsection{Question 18: Adding Phase for Continuity}

\textbf{Include phase in synthesis. Are transitions between frames perfect?}

Phase continuity is achieved by maintaining a global sample counter and using the FFT phase:
\begin{equation}
y[n] = \sum_{h} A_h \cos\left(2\pi f_h \frac{n_{global}}{f_s} + \phi_h\right)
\end{equation}

where $n_{global}$ is the total sample count since program start, and $\phi_h$ is the phase from the FFT. This ensures the synthesized waveform continues smoothly from one frame to the next.

Transitions are improved but not perfect. Imperfections arise from:
\begin{itemize}
\item $f_0$ variations between frames causing frequency jumps
\item Amplitude changes between consecutive frames
\item FFT phase estimation errors
\item Frame-to-frame harmonic content variations
\end{itemize}

For sustained vowels with stable $f_0$, transitions are nearly seamless. During transients or pitch changes, some artifacts may remain.

\section{Conclusion}

This project implements a real-time autotune effect using additive synthesis. Key challenges addressed include separating non-real-time operations (file I/O, memory allocation) from the audio callback, implementing $f_0$ estimation via autocorrelation, balancing buffer size trade-offs between latency and accuracy, performing FFT-based harmonic analysis, and synthesizing audio with phase continuity to avoid frame boundary artifacts.

The final implementation processes audio in real-time, detecting pitch, extracting harmonics, and reconstructing the signal through additive synthesis, demonstrating the critical importance of distinguishing real-time from non-real-time operations and maintaining phase coherence across frames.

\end{document}
